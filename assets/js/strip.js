/* ═══════════════════════════════════════════════
   STRIP.JS — Image Filmstrip Carousel
   FORM/GROUND Multi-Studio Website
   ═══════════════════════════════════════════════ */

(function () {
  // Animation is CSS-driven via @keyframes stripScroll.
  // Pause/resume is handled by CSS :hover on .image-strip.
  // This file exists for any future JS-driven enhancements
  // (e.g., touch/drag, dynamic slide injection).

  var strip = document.querySelector('.image-strip');
  if (!strip) return;

  // Slides are pre-duplicated in HTML for seamless loop.
  // No additional JS duplication needed.
})();
